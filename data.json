{
  "_note": "This root-level data.json is unused. The deck reads from src/data.json (during dev) and from the bundled JSON inside dist/index.html (after build). Edit src/data.json — never edit this file."
}
